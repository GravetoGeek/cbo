// list-familias.query.ts

/**
 * Query que representa a operação "listar todas as famílias".
 * Nesse caso, não precisamos de parâmetros,
 * mas poderíamos colocar filtros, paginação, etc., se desejarmos.
 */
export class ListFamiliasQuery {}
