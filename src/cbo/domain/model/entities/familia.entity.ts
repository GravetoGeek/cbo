export class Familia {
    id: string
    codigo: string
    titulo: string
    constructor(id: string, codigo: string, titulo: string) {
        this.id = id
        this.codigo = codigo
        this.titulo = titulo
    }
}

