export class Sinonimo {
    id?: string
    codigo: string
    titulo: string
    constructor(codigo: string, titulo: string) {
        this.codigo = codigo
        this.titulo = titulo
    }
}
